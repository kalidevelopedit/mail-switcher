kind = "web"
previewPath = "/"
title = "AuthStudio"
version = "1.0.0"
id = "artifacts/auth-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25089

[services.development]
run = "pnpm --filter @workspace/auth-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/auth-studio", "run", "build" ]
publicDir = "artifacts/auth-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25089"
BASE_PATH = "/"
